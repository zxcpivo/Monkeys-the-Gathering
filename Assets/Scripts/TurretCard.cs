using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurretCard : BaseSupportCard
{
    public TurretCard(TurnSystem turnScript, string name, int attack, int health, int cost) : base(turnScript, name, attack, health, cost)
    {
        
    }

    public override void ActivateEffect()
    {
        
    }
}
